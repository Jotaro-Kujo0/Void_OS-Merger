/*
 * worker/src/capabilities.c — worker capability placeholder.
 *
 * TODO:
 *
 * - Implement platform-specific probes behind a stable shared interface.
 * - Build join and heartbeat payloads from snapshot data.
 * - Avoid placing platform-specific code in the master scheduler.
 * - Add fixtures for Linux, Android, low-power, and display-less workers.
 */
