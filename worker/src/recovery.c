/*
 * worker/src/recovery.c — worker recovery placeholder.
 *
 * TODO:
 *
 * - Stop new work during drain.
 * - Reconcile active chunks after reconnect.
 * - Honor lease revocation and cancellation.
 * - Preserve only results that the master can associate with a valid lease.
 */
