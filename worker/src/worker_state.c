/*
 * worker/src/worker_state.c — local worker state placeholder.
 *
 * TODO:
 *
 * - Implement the lifecycle described by worker_state.h.
 * - Keep local observations timestamped.
 * - Emit validated events for transport and executor coordination.
 * - Add tests for reconnect, draining, and shutdown transitions.
 */
