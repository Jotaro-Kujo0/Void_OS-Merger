# Logical-device UI boundary — planning scaffold only.
#
# No executable target is defined yet. The first presentation may be the
# master control CLI or a terminal UI. Future graphical frontends should read
# ui/model.h rather than reaching into scheduler or worker internals.
#
# TODO:
#
# - Select a presentation technology after the view-model is stable.
# - Add an optional target when the first UI is ready.
# - Preserve structured output and accessibility-oriented interaction.
