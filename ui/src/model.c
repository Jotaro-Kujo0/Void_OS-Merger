/*
 * ui/src/model.c — presentation-neutral view-model placeholder.
 *
 * No implementation is intentionally present.
 *
 * TODO:
 *
 * - Convert master-owned state snapshots into stable UI records.
 * - Keep formatting and event-loop concerns in the eventual UI frontend.
 * - Ensure CLI and future graphical clients see consistent state.
 * - Add tests for empty, active, degraded, and recovering logical devices.
 */
