#!/usr/bin/env bash
# scripts/run_tests.sh — build the configured project and run registered tests.

set -euo pipefail

# TODO: configure with -DVOM_BUILD_TESTS=ON.
# TODO: build cluster-master, cluster-worker, vom-cli, and all tests.
# TODO: invoke ctest --output-on-failure from build/linux.
